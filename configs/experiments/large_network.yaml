# Experiment: Large Network (more capacity, slower training)

training:
  episodes: 100
  policy_update_epochs: 10

network:
  actor_hidden_size: 256
  critic_hidden_size: 512
  dropout_rate: 0.1

learning:
  lr_actor: 0.0001
  lr_critic: 0.0001

ppo:
  batch_size: 32768
